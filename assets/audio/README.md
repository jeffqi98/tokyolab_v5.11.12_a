Place your licensed audio file here:

```text
from-the-start.mp3
```

The site music button reads:

```text
assets/audio/from-the-start.mp3
```

Browsers require a user click before playing audio with sound, so the floating music button starts playback on the first tap/click.
